name: Sync Crowdin

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - dev-rust
    paths:
      - crates/editor/ui/**/*.slint
      - crowdin.yml
      - .github/workflows/sync_crowdin.yml

jobs:
  sync:
    name: Sync
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2

      - name: Install slint-tr-extractor
        run: cargo install slint-tr-extractor

      - name: Extract translation strings
        run: |
          slint-tr-extractor crates/editor/ui/*.slint -o crates/editor/i18n/editor.pot

      - name: Sync translations with Crowdin
        uses: crowdin/github-action@v2
        with:
          config: crowdin.yml
          upload_sources: true
          upload_translations: false
          download_translations: true
          push_translations: false
          push_sources: false
        env:
          CROWDIN_PROJECT_ID: ${{ secrets.CROWDIN_PROJECT_ID }}
          CROWDIN_PERSONAL_TOKEN: ${{ secrets.CROWDIN_PERSONAL_TOKEN }}

      - name: Commit translations
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "i18n: sync translations from Crowdin"
